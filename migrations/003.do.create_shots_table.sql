CREATE TABLE shots (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    shot_name TEXT, 
    shot_isCompleted BOOLEAN NOT NULL,
    dog_id INTEGER REFERENCES dogs(id) ON DELETE SET NULL
);
